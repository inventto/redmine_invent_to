<%= raw @issues.map {|issue| {
      'id' => issue.id,
      'label' => "#{issue.tracker} ##{issue.issue_number.number rescue issue.id}: #{truncate issue.subject.to_s, :length => 60}",
      'value' => issue.id
      }
    }.to_json
%>
